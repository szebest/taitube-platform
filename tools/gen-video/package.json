{
  "name": "@vp/gen-video",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "gen": "node --experimental-strip-types src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@vp/testing": "workspace:*",
    "@vp/tsconfig": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^3.0.7"
  }
}
