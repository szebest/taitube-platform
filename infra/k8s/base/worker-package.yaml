apiVersion: apps/v1
kind: Deployment
metadata:
  name: vp-worker-package
  namespace: video-pipeline
  labels:
    app.kubernetes.io/name: vp-worker-package
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: video-pipeline
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: vp-worker-package
  template:
    metadata:
      labels:
        app.kubernetes.io/name: vp-worker-package
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: video-pipeline
    spec:
      terminationGracePeriodSeconds: 120
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: worker
          image: ghcr.io/szebest/vp-worker:latest
          command: ["sh", "-c", "exec ${WORKER_RUNTIME:-bun} --import ./dist/instrument.js dist/main.js"]
          lifecycle:
            preStop:
              exec:
                command: ["sleep", "5"]
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: metrics
              containerPort: 9464
          envFrom:
            - configMapRef:
                name: vp-config
            - secretRef:
                name: vp-secrets
          env:
            - name: WORKER_STAGE
              value: package
            - name: FFMPEG_THREADS
              valueFrom:
                resourceFieldRef:
                  resource: limits.cpu
          resources:
            requests:
              cpu: 500m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          volumeMounts:
            - name: tmp
              mountPath: /tmp/vp
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - test -f /tmp/vp/heartbeat && test $(( $(date +%s) - $(cat /tmp/vp/heartbeat) )) -lt 45
            periodSeconds: 30
            initialDelaySeconds: 15
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /readyz
              port: metrics
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 3
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: vp-worker-package
  namespace: video-pipeline
  labels:
    app.kubernetes.io/name: vp-worker-package
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: video-pipeline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vp-worker-package
  ports:
    - name: metrics
      port: 9464
      targetPort: 9464
